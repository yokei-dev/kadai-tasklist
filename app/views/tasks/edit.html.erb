<h1>タスクの編集</h1>

<h2>編集前のタスク</h2>
<h2><%=@task.id%></h2>
<h2><%=@task.content%></h2>

<%= form_with(model: @task, local: true) do |f| %>
    <%= f.label :content, 'タスク' %>
    <%= f.text_field :content %>

    <%= f.submit '編集' %> 
<% end %>


<%= link_to '詳細に戻る', task_path %>
